const destructureData = ({
  id,
  name,
  email,
  userName,
  profileImage,
}) => {
    return {
      id,
      name,
      email,
      userName,
      profileImage,
    };
};

export default destructureData;
