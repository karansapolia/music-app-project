import React from 'react';
import { Form, Input } from '../common/helpers';

const FormInput = props => <Form.Field fluid iconPosition="left" control={Input} {...props} />;

export default FormInput;
